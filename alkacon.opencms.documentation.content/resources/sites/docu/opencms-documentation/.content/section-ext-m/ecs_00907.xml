<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[How content is rendered]]></Title>
    <Section>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>All HTML rendered in OpenCms is rendered via JSPs. Thus, also the HTML that displays content is rendered via JSPs. A JSP that renders content is called a <em>formatter</em>. A formatter is configured via a <em>formatter configuration</em>. The configuration specifies which content of which type is rendered with which formatter in which containers.</p>
<div class="alert alert-danger">Content items that should be rendered via formatters must be configured as resource types.</div>
<p>In addition to information about where to use a formatter, the configuration can contain information about special settings available for the formatter. These are called 'element settings'.</p>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
