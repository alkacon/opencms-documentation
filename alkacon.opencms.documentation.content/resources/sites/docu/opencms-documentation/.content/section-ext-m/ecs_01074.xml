<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[Basic definitions]]></Title>
    <Section>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>We will call a container page <code>a.html</code> a <strong>descendant page</strong> of another container page <code>b.html</code> if the parent folder of <code>a.html</code> is a direct or indirect subfolder of the parent folder of <code>b.html</code>. Conversely, we call <code>b.html</code> an <strong>ancestor page</strong> of <code>a.html</code>. When editing a container page, we call the parent folder of that page the <strong>current folder</strong>. This is important because inheritance group data is attached to folders, not to individual container pages. So container pages in the same folder can't have different content for the same inheritance group.</p>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
