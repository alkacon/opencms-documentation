<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[Saving values from the form to the content 3]]></Title>
    <Section>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>Saving is managed by <code>ugc.saveContent(...)</code>. It takes the updated mapping as the first argument and a handler that should be called after successfully storing the XML content. The handler typically destroys the UGC session and replaces the input form by a success notification, or redirects to another web page.</p>
<p>Note that the handler has an argument: <code>uploadResult</code>. This parameter contains the VFS URIs of the uploaded files given in the order of upload. The URIs must be written to the XML content elements that should link to the uploaded files. Hence, the mapping values must be adjusted. You have to write a function <code>updateValues(...)</code> that handles these adjustments. Here is an example:</p>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
