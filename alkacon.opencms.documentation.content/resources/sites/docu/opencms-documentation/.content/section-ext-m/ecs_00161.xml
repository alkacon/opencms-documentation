<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[ The benefit of value wrappers by example 3]]></Title>
    <Section>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>In the improved formatter, value wrappers are used:</p>
<ol>
<li>access the element setting via<code> cms.element.setting</code> (<code>setting</code> without 's' in the end in comparison to the first formatter) and make it a boolean value by means of <code>toBoolean</code> expression.</li>
<li>access the image as image bean by using <code>content.value.Image.toImage</code> and directly scale it and output the <code>src</code>, <code>width</code> and <code>height</code> attribute of the <code>&lt;img&gt;</code> tag by calling <code>imgSrc</code>. Hence, the correctly scaled image is delivered to the browser.</li>
</ol>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
