<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[The idea of nested containers]]></Title>
    <Section>
      <Text name="Text0">
        <links>
          <link name="link0" internal="true" type="A">
            <target><![CDATA[/sites/docu/opencms-documentation/creating-a-website/creating-a-template-jsp/]]></target>
            <uuid>95522bfc-d1c7-11ec-9238-0242ac11002b</uuid>
          </link>
        </links>
        <content><![CDATA[<p>Containers are holes in a template where content can be dropped. When writing a template JSP in OpenCms, you use the <code>&lt;cms:container&gt;</code> tag to add such holes. See&nbsp;<a href="%(link0)">here</a> to find out how to write a template JSP. Nested containers are also such holes where content can be dropped. But they are not defined in the template JSP. They are defined in a content's formatter. If you add content rendered by such a formatter to your page, it will make up a new container inside the container where you place the content. Hence, a nested container. To distinguish nested containers from the containers defined in a template, we call the latter ones <em>top-level containers</em>.</p>
<p><em>Nested containers are not part of the content itself.</em> If you place the content that renders the nested container again on your page, or on a different page, the elements in the nested container will not be inserted. For each element-instance of the content that renders the nested container, the container is completely independent.</p>
<p><em>Nested containers are tied to a container page element.</em> If you move a content that renders a nested container on a page (and the formatter does not change), the element in the nested container will move with it. If you remove the content, or change its formatter, the nested container will vanish and its elements will not be visible anymore.</p>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
