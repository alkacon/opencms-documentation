<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[Dealing with synchronized values]]></Title>
    <Section>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>For each content type, it can be configured that special fields of the content should be the same in all languages. This may in particular be used for images or links.</p>
<div class="alert alert-info">If synchronization is configured for some fields, altering a value in one language will alter the value in all languages.</div>
<p>If a content is edited outside of the form-based or the inline editor, it may happen that usually synchronized values are out of sync. To sync them again, use the "Synchronize" button in the "Copy locale" dialog. Then all fields that should be synchronized are set to the values of these fields in the currently edited language. Note that the "Synchronize" button only appears in the "Copy locale" dialog if there are synchronized fields defined.</p>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
