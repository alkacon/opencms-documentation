<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[Server-side validation of values 2]]></Title>
    <Section>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>Calling <code>ugc.validate(...)</code> will send the values of the mapping (that we update with the function also used for saving values) given as the first argument to the server and checks if the values can be stored in the XML content. The <code>validationHandler</code> given as the second argument is called when the validation is performed and it gets a map of validation errors as an argument. A handler implementation could look like:</p>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
