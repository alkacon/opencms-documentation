<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[Specifying the search fields type 3]]></Title>
    <Section>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>In the example, the element <code>Title</code> will be indexed as field of type <code>text_{locale}</code> (default) and additionally as field of type <code>string</code> due to the <code>*_s</code> notation. For the english locale, for example, the result would be the following:</p>
<ul>
<li>the field <code>atitle_en</code>, that contains the content of the <code>Title</code> element processed as text according to the english locale</li>
<li>the field <code>atitle_en_s</code>, that contains a one-to-one copy of the value of the <code>Title</code> element</li>
</ul>
<p>In the <code>copyfields</code> attribute one can provide more than one field by separating the respective fields by comma.</p>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
