<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[Configuration of the default template context provider 2]]></Title>
    <Section>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>For each context you can configure:</p>
<ul>
<li>a (optionally languguage-specific) nice name</li>
<li>a path to the template that should be used for the context <code></code></li>
<li>device variants for each context, each with (langugage specific) nice names and display resolutions specified by width and heigth.</li>
</ul>
<p>In the config-file you can use macros to access provider parameters. In the given default configuration the <code>template</code> parameter is accessed via <code>%(template)</code>. You could also call the provider with different parameters. For example, you could provide different templates for different contexts.</p>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
