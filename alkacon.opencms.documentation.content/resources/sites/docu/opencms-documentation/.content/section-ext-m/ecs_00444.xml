<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[Starting the auto-setup 2]]></Title>
    <Section>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>In the command we use two variables:</p>
<ul>
<li><code>$OPENCMS</code> for the path to OpenCms' web-app root folder. For example, on an Ubuntu Server (14.04) with a standard Tomcat installation and OpenCms deployed as the ROOT app, the path would be <code>/var/libs/tomcat9/webapps/ROOT/</code>.</li>
<li><code>$CONFIG_FILE</code> for the path to the configuration file used by the headless installation.</li>
</ul>
<div class="alert alert-info">Executing the auto-setup may take some time, especially when it comes to the modules import. Wait until you get a status update from the setup program.</div>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
