<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[Fields of the "Add resource type" dialog 2]]></Title>
    <Section>
      <Text name="Text0">
        <links>
          <link name="link0" internal="true" type="A">
            <target><![CDATA[]]></target>
            <anchor><![CDATA[further-resource-type-configuration-322bb7e5]]></anchor>
          </link>
        </links>
        <content><![CDATA[<p>If you click “Ok” the content type is created and configured as a resource type. In particular, the following actions are performed, where <code>&lt;name&gt;</code> is a placeholder for the resource type’s name:</p>
<ul>
<li>In the provided "Parent folder of schema", by default the <code>schemas/</code> subfolder of the chosen module, the file <code>&lt;name&gt;.xsd</code> is created. The file defines a sample content type.</li>
<li>In the provided "Parent folder of formatter", by default the <code>formatters/</code> subfolder of the current module, the file <code>&lt;name&gt;.jsp</code> is created. The file defines a sample formatter JSP for the content type.<br />Furthermore, a file <code>&lt;name&gt;.xml</code> is created. It configures the created formatter JSP to be used for the created content type in any container.</li>
<li>In the message bundle file provided as "Message bundle" the keys
<ul>
<li><code>type.&lt;name&gt;.name</code></li>
<li><code>type.&lt;name&gt;.title</code></li>
<li><code>type.&lt;name&gt;.description</code></li>
<li><code>type.&lt;name&gt;.formatter</code></li>
</ul>
are added and, the values given as “Display name” and “Description” are set (the second for <code>type.&lt;name&gt;.description</code>, the first for the three other keys).</li>
<li>The content type is added as resource type to the module configuration file provided as "Module config".</li>
<li>The content type is configured as resource and explorer type in the file <code>opencms-modules.xml</code>, located in the RFS in the folder <code>WEB-INF/config/</code>.</li>
<li>All files created in the VFS are added as module resources.</li>
<li>All changes made in the VFS get published.</li>
</ul>
<p>The newly available resource type can now be added to any container via drag &amp; drop as any resource type available before. To personalize the created resource type you should:</p>
<ul>
<li>Alter the schema (<code>&lt;name&gt;.xsd</code>).</li>
<li>Adjust the formatter and the formatter config (<code>&lt;name&gt;.jsp</code> and <code>&lt;name&gt;.xml</code>).</li>
<li>Configure the icons for the resource type with your own icons. Therefor see the section about <a href="%(link0)">further resource type configuration</a>.</li>
<li>Optionally, create and configure additional formatters.</li>
<li>Optionally, add values for further languages to the respective message bundle files.</li>
</ul>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
