<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[Additional features of the bundle editor]]></Title>
    <Section>
      <Caption><![CDATA[Lazy locking]]></Caption>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>Since OpenCms 10.5.1, the bundle editor locks the opened files lazily. A lock is placed only when you really start editing data stored in the specific file. Whenever an input field in the editor's table loses it's focus, it is checked if the value has changed. You will be warned, when changes are not possible because locking failed. In this case the original value contained in the input field will be restored.</p>
<p>Lazy locking allows you in particular to translate to different languages in parallel when using property resource bundles.</p>]]></content>
      </Text>
    </Section>
    <Section>
      <Caption><![CDATA[Key modification behavior]]></Caption>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>When you edit <strong>property bundles, the property <code>content-encoding</code> is set at the file on save</strong>. This prevents encoding problems when you export your bundle from one system and import it to another system with different default encoding.</p>
<p>When a bundle uses a descriptor and a bundle file contained <strong>messages with keys not declared in the descriptor</strong>, these <strong>are removed on save</strong>.</p>
<div class="alert alert-info">Note that this remove operation happens only for translations you actually edited, other translations are simply not rewritten.</div>]]></content>
      </Text>
    </Section>
    <Section>
      <Caption><![CDATA[Behavior on save ]]></Caption>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>When you edit <strong>property bundles, the property <code>content-encoding</code> is set at the file on save</strong>. This prevents encoding problems when you export your bundle from one system and import it to another system with different default encoding.</p>
<p>When a bundle uses a descriptor and a bundle file contained <strong>messages with keys not declared in the descriptor</strong>, these <strong>are removed on save</strong>.</p>
<div class="alert alert-info">Note that this remove operation happens only for translations you actually edited, other translations are simply not rewritten.</div>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
