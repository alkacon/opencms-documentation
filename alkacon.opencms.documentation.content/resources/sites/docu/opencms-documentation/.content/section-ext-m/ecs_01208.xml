<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[Image drag-and-drop]]></Title>
    <Section>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>Image drag-and-drop is another helpful feature that can ease content authoring significantly. In some phases of a website's livetime it may be necessary to replace many or maybe even all images of a website. Inline editing would only be of small help here, because a dialog still has to be opened for each image. This again disturbs the workflow of content editors. With the image drag-and-drop feature, an image on a web page can be replaced directly with another image from the gallery dialog by dragging and dropping. This means that the gallery dialog has to be opened once only, and after that, all images on a page can be replaced conveniently.</p>
<p>Since it is relatively easy to enable image drag-and-drop, and the feature is of great value for content editors, you should not miss to support this opportunity in your formatter JSP.</p>
<p>Image drag-and-drop can be enabled via a <code>content.imageDnd[content.value.Image.path]</code> annotation, either on&nbsp;the <code>&lt;img&gt;</code> tag itself or at a surrounding <code>&lt;div&gt;</code> tag. It has always the form <code>${content.imageDnd[&lt;XPath to the&nbsp;<a style="box-sizing: border-box; color: inherit; text-decoration: none; background-color: transparent;" tabindex="0" role="button" data-container="body" data-toggle="popover" data-placement="bottom" data-content="&lt;p&gt;In an XML schema definition, the elements of types are called 'schema elements'.&lt;/p&gt;" data-trigger="focus"><abbr title="Click to see description">schema element</abbr></a> of the image&gt;]}</code> <code></code>where <code>path</code> is the one you get via <code>content.value.&lt;ImageElement&gt;.path</code>.</p>
<p>As with inline editing, image drag-and-drop only works for nonempty image fields.</p>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
