<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[Defining the visibility of a schema element 2]]></Title>
    <Section>
      <Text name="Text0">
        <links>
          <link name="link0" internal="true" type="A">
            <target><![CDATA[/sites/docu/opencms-documentation/content-in-opencms/defining-the-schema-of-a-content-type/defining-the-contents-behaviour/field-settings/]]></target>
            <uuid>069bfb76-d1c9-11ec-9238-0242ac11002b</uuid>
          </link>
        </links>
        <content><![CDATA[<p>Nested in the node <code>xsd:schema/xsd:annotation/xsd:appinfo/visibilities</code> we provide a node <code>&lt;visibility element="NotForEveryOne" params="Administrators|Projectmanagers|ROLE.GALLERY_EDITOR"&gt;</code>. It says that the element named <code>NotForEveryOne</code> is only visible for members of one of the groups provided in the attribute <code>params</code>, and for users that have at least one of the listed roles.</p>
<p>The attribute <code>params</code> configures the visibility handler. The default handler takes a pipe (<code>|</code>)-separated list of group names and role identifiers. For users that are members of at least one of the listed groups (or that have at least one of the listed roles), the content element is displayed in the editor. For all other users it is hidden. Consequently, if you leave the list empty, the element is never displayed.</p>
<p>The <code>&lt;visibility&gt;</code> node takes a third, optional, argument class. If you want to replace the default visibility handler, provide the qualified class name of handler you want to use. Note that a non-default handler will usually accept different <code>params</code> values than the default handler.</p>
<div class="alert alert-info">These configurations can also be done in the preferable field-centered syntax with the <a href="%(link0)">field settings</a>, introduced in OpenCms 11.</div>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
