<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[Defining the visibility of a schema element 2]]></Title>
    <Section>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>The visibility setting <code>Administrators|Projectmanagers|ROLE.GALLERY_EDITOR</code> defined for the <code>NotForEveryOne</code> field defines that the element named <code>NotForEveryOne</code> is only visible for members of one of the defined groups or roles. The <code>&lt;Visibility&gt;</code> setting internally uses the the default visibility handler. The default handler takes a pipe (<code>|</code>)-separated list of group names and role identifiers. For users that are members of at least one of the listed groups or that have at least one of the listed roles, the content element is displayed in the editor. For all other users it is hidden.</p>
<p>Consequently, if you leave the setting empty or you define an arbitrary value such as <code>hidden</code>, that is not a group name or role name, the element is never displayed as for the example's <code>ForNobody</code> field.</p>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
