<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[Content specific localization of the content editor 2]]></Title>
    <Section>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>The keys can either be added to the <code>{module name}.workplace</code> resource bundle that's included in the module where the resource type is defined in, or they can reside in a separate bundle, usually called <code>{module name}.{resource type name}</code>, also located in the module's <code>i18n/</code> subfolder. The second option is the preferred one, but it requires you to make the message bundle explicitly available in the schema of the content (the XSD) by adding the following subnode to <code>&lt;xsd:annotation&gt;/&lt;xsd:appinfo&gt;</code>:</p>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
