<?xml version="1.0" encoding="UTF-8"?>

<ExtendedSectionData xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/alkacon.mercury.extension.section_ext/schemas/section-ext.xsd">
  <ExtendedSection language="en">
    <Title><![CDATA[The content_{locale} field]]></Title>
    <Section>
      <Text name="Text0">
        <links/>
        <content><![CDATA[<p>The index fields <code>content_{locale}</code> are special fields. They collect all textual data of one language of a content into a common field and so to say represent the language specific content of a resource. For each content element one can decide if the textual data should be added to the <code>content_{locale}</code> fields or not. This is done by the optional attribute <code>searchcontent</code>, available for the <code>&lt;searchsetting&gt;</code> tag. The default value is <code>true </code>indicating that the element's value is added to the <code>content_{locale}</code> fields.</p>
<p>Here's an example configuration, where <code>Date</code> is not added to the <code>content_{locale}</code> field whilst <code>Title</code> and <code>Text</code> are added.</p>]]></content>
      </Text>
    </Section>
  </ExtendedSection>
</ExtendedSectionData>
