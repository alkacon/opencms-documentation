<?xml version="1.0" encoding="UTF-8"?>

<DocumentationHtmls xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/com.alkacon.opencms.documentation/schemas/documentation-html.xsd">
  <DocumentationHtml language="en">
    <Html name="Html0">
      <links/>
      <content><![CDATA[<p>where the arguments of the constructor are:</p>
<ul>
<li>the <code>id</code> attribute of the <code>&lt;form&gt;</code> that should be connected to the content,</li>
<li>a function that initializes the mapping between content elements and form fields,</li>
<li>a handler for server-side errors,</li>
<li>(optional) a handler called when the form is initialized (i.e., when content elements are written to the form)</li>
<li>(optional) a wait handler, called when data is send/received and when sending/receiving is completed.</li>
</ul>
<p> Initialization must be wrapped in the function <code>window.initUgc</code> that is called, when the native API is ready:</p>]]></content>
    </Html>
  </DocumentationHtml>
</DocumentationHtmls>
