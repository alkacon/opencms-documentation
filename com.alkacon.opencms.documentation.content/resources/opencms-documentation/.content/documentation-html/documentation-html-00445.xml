<?xml version="1.0" encoding="UTF-8"?>

<DocumentationHtmls xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/com.alkacon.opencms.documentation/schemas/documentation-html.xsd">
  <DocumentationHtml language="en">
    <Html name="Html0">
      <links/>
      <content><![CDATA[<p>Copy the <code>opencms.war</code> file from the binary distribution ZIP file to <code>CATALINA_HOME/webapps/</code>. Replace <code>CATALINA_HOME</code> with the real path to your Tomcat installation.</p>
<p>Start (or restart) Tomcat. Tomcat will now deploy the web application OpenCms.</p>
<div class="alert alert-danger">OpenCms requires that it's <code>*.war</code> file is unpacked. OpenCms can not be deployed as war file only. Make sure Tomcat does unpack the war file and creates the <code>CATALINA_HOME/webapps/opencms/</code> directory, placing the OpenCms files in this directory. The default configuration for your Servlet containers / environment could be to not unpack the deployed <code>*.war</code> file. If this is so, you must unpack the opencms.war file manually. Use an unzip tool for this, <code>*.war</code> files are just <code>*.zip</code> files with a different extension. The OpenCms setup wizard will display a warning and not allow you to continue if you did not unpack the <code>*.war</code> file.</div>]]></content>
    </Html>
  </DocumentationHtml>
</DocumentationHtmls>
