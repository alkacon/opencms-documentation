<?xml version="1.0" encoding="UTF-8"?>

<DocumentationHtmls xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/com.alkacon.opencms.documentation/schemas/documentation-html.xsd">
  <DocumentationHtml language="en">
    <Html name="Html0">
      <links>
        <link name="link0" internal="false" type="A">
          <target><![CDATA[http://www.mysql.com/downloads/]]></target>
        </link>
      </links>
      <content><![CDATA[<p>Install the "MySQL Community Server" from <code> <a title="External link, opens in new window" href="%(link0)" target="_blank">http://www.mysql.com/downloads/</a></code> (see under "MySQLCommunity edition" for the free version).</p>
<p>OpenCms can be used with MySQL 5.0, 5.1, 5.5, 5.6 and 5.7.</p>
<p>Start the MySQL server by running the service (WINDOWS) or executing <code>%MYSQL_HOME%/bin/mysqld</code> (UNIX).</p>
<p>Check that MySQL is running before you continue by starting the MySQL monitor (execute <code>mysql</code> in your MySQL bin folder). The database works correctly if a MySQL prompt appears after calling the monitor. Quit the MySQL monitor by typing exit and go to the next step.</p>
<div class="alert alert-danger">You will have to increase the MySQL configuration variable <code>max_allowed_packet</code> located in the MySQL configuration file (usually called <code>my.ini</code>). For OpenCms, the limit should be as high as possible, a setting of at least <code>max_allowed_packet=32M</code> is recommended.</div>]]></content>
    </Html>
  </DocumentationHtml>
</DocumentationHtmls>
