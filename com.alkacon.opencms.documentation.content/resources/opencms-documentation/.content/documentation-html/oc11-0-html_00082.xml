<?xml version="1.0" encoding="UTF-8"?>

<DocumentationHtmls xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/com.alkacon.opencms.documentation/schemas/documentation-html.xsd">
  <DocumentationHtml language="en">
    <Html name="Html0">
      <links/>
      <content><![CDATA[<p>The interesting parts where value transformers make your code nicer are:</p>
<ol>
<li>the check if the element setting <code>showImage</code> is <code>true</code> (line 8),</li>
<li>the part where the image link is rendered (line 9).</li>
</ol>
<p>For the setting, that is probably visible as a check box in the settings dialog, it would be nice to access it as boolean value, since we know it is a boolean value. For the image, it would be nice to access it as image bean, since we know it is an image and we might want to scale it or add correct width and height attributes, or even produce a whole source set of images from the provided one. Our initial formatter will return the URL to the unscaled image and the browser will have to resize the image to width of 400 pixels.</p>
<p>With value wrappers, we can get the information typed as we like. Here's an adjusted version of the formatter using the wrappers:</p>]]></content>
    </Html>
  </DocumentationHtml>
</DocumentationHtmls>
