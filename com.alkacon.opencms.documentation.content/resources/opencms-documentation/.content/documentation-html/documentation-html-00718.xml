<?xml version="1.0" encoding="UTF-8"?>

<DocumentationHtmls xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/com.alkacon.opencms.documentation/schemas/documentation-html.xsd">
  <DocumentationHtml language="en">
    <Html name="Html0">
      <links/>
      <content><![CDATA[<p>In this approach you have just one folder tree where each page is by itself multilingual. To obtain the multi-lingual behavior, you have to use the <code>__locale</code> parameter to set the locale - as alternative to using the <code>locale</code> property. At the moment the approach is possible, but not yet completely supported. You have to take care for creating correct links by yourself: either by attaching the <code>__locale</code> parameter manually (in the formatters, etc.) to each link, or by substituting the default link substitution handler to a version that adds the parameter automatically.</p>
<p>Advantages:</p>
<ul>
<li>You have to maintain only one site</li>
<li>New pages are directly available in all languages when added</li>
</ul>
<p>Drawbacks:</p>
<ul>
<li>You have to take care of attaching the <code>__locale</code> parameter to links</li>
<li>Your URLs are the same for each language version</li>
<li>Properties are uni-lingual, which may cause you problems in particular with navigations.</li>
</ul>]]></content>
    </Html>
  </DocumentationHtml>
</DocumentationHtmls>
