<?xml version="1.0" encoding="UTF-8"?>

<DocumentationHtmls xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="opencms://system/modules/com.alkacon.opencms.documentation/schemas/documentation-html.xsd">
  <DocumentationHtml language="en">
    <Html name="Html0">
      <links/>
      <content><![CDATA[<p>The most interesting part is the <code>&lt;cms:container&gt;</code> tag. Using this tag, the formatter exposes a new container. Setting the (optional) attribute <code>editableby</code>, it restricts the users that are able to add or remove contents from the exposed container. Via <code>tagClass</code> we add a css class to the HTML-tag (<code>div</code>) that is rendered for the container. In the example, the gray borders are added by that class. The <code>type</code> attribute is used to specify which formatters can be used in the container and the name attribute becomes (prefixed with the layout elements id) the id of the container in the container page, and the HTML-tag rendered by the container tag.</p>
<p>In the body of the <code>&lt;cms:container&gt;</code> tag we placed some HTML. It is shown whenever the container is empty.</p>]]></content>
    </Html>
  </DocumentationHtml>
</DocumentationHtmls>
